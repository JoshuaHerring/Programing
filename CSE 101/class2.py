# typing a backslash before a single qoute in a sentence \' will nullify the quote and it will
#appear in a print funcition but it will not register as code
#a backslash is an escape sequence. A backslash can backslash another backslash
# if you type \n it will continue the same code on a new line.
print('there are so many things to do \n so we need more lines')
dog="""there are so many things that we can do
fun fact if yiou use a triple double quote you can
just keep on typing forever untill you end it with
a triple double quote""".upper()
print(dog)